; (library
;  (name toy))

; (executable
;  (name main)
;  (modules main lexer parser sexpr ast interp util)
;  (libraries core core_unix.command_unix fmt)
;  (preprocess
;   (pps ppx_jane)))

(library
 (name toy)
 ; (modules parse_to_eval)
 (libraries core fmt ast parsing))
