(define times2
    (lambda (x)
        (+ x x)))

(define loop
    (lambda (start end f)
        (if (= start end)
            #u
            (block (print (f start))
                   (loop (+ start 1) end f)))))

(loop 0 10 times2)